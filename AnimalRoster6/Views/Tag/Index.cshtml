@model List<AnimalRoster6.Models.Tag>

<h1>All Event Tags</h1>

@if (Model.Count == 0)
{
    <p>No tags yet!</p>
}
else
{
    <table class="table table-striped table-bordered">
        <tr>
            <th scope="col">ID
            <th scope="col">Tag Name</th>
        </tr>
        @foreach (Tag tag in Model)
        {
            <tr>
                <td>@tag.Id</td>
                <td>@tag.Name</td>
            </tr>
        }
    </table>
}